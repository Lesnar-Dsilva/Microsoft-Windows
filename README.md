- cleanup.bat SHOULD be ran on a monthly-basis to keep your system in working condition, RESET the system, if this doesn't help.

- winaudit.ps1 this is a tool you can run on a Windows machine (**powershell -ExecutionPolicy Bypass -File winaudit.ps1**) as ADMIN, to check the system spec and security.
